{
    "configurations": [
        {
            "name": "Docker Node.js Launch",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "platform": "node",
            "node": {
                "package": "${workspaceFolder}/backend/postgresql-express-batch/package.json",
                "localRoot": "${workspaceFolder}/backend/postgresql-express-batch"
            }
        }
    ]
}